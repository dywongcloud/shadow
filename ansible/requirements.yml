---
# ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix     # synchronize, firewalld
    version: ">=1.5.0"
  - name: community.general # git_config
    version: ">=8.0.0"
