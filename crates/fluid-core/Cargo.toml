[package]
name = "fluid-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
hive-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }
regex = "1"
# BLAKE3 is the platform's content-addressing hash (browser artifact digests).
# Already in the workspace lock (hive-browser, guardian-db) at 1.8.x.
blake3 = "1"
# SHA-256 anchors the normalized-manifest identity every deployment generation
# binds (`canonical_manifest_bytes` / `normalized_manifest_sha256`).
sha2 = { workspace = true }
