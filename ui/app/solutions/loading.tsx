import { PageSkeleton } from "@/components/page-skeleton";

// Instant responsive skeleton shown while this route segment loads (all devices).
export default function Loading() {
  return <PageSkeleton />;
}
