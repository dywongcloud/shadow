
## 2026-07-29 -- residual-scan vs CONSOLIDATE blocked-rows stuck loop, THIRD recurrence — first real resolution (re-home)
Goal (G): formally close a fully-converged gm chain (DNS/ACME hardening shipped, zero reachable rows) without violating gate semantics.
What drifted / what went wrong: the same structural deadlock as 2026-07-18 and 2026-07-22 below, this time one gate layer earlier — residual-scan refuses while ANY PRD row is pending ("complete or remove them"), CONSOLIDATE requires the scan's fired marker in the same stop window, and the 11 remaining rows were all legitimately blocked (user decisions, physical access, upstream bugs, account-owner access). 5 identical transition denials across two sessions (stuck-loop-escalation).
Fix / resolution: stopped bare-retrying per BBCR (bound is 2-3; we were at 5); probed one differently-shaped variant (`acknowledge_blocked` body — a no-op); surfaced to the owner with three real options (re-home rows / fix the rs-plugkit gate upstream / park the chain). Owner chose re-home: 11 rows moved VERBATIM into tracked `docs/blocked-work.md`, and — the new move vs the two prior recurrences — both `blockedBy: user decision` rows were decided on the spot via AskUserQuestion (Minecraft EULA accepted, crash-loop closed; -5515¢ bug-induced billing debt credited to zero). Chain walked VERIFY→CONSOLIDATE→COMPLETE cleanly. The upstream gate disagreement stays filed in the carry file.
Generalizes to: this deadlock has now recurred three times — it is the default end-state of any long chain with genuinely-external rows, so expect it at every closure and budget the re-home (or the upstream fix) as a normal step, not an exception. `blockedBy: user decision` rows unblock instantly when actually asked — they are usually a missing AskUserQuestion, not a real blocker. And tooling self-recovery has a documented floor: when the shared agentplug daemon wedges mid-takeover (live heartbeat, zero dispatch processing), `~/.gm-tools/agentplug-runner dispatch gm <verb> '<json>'` is the working single-shot path.

## 2026-07-18 — gm CONSOLIDATE gate has no accommodation for blockedBy:external PRD rows
Goal (G): Reach genuine gm COMPLETE, or correctly recognize/communicate that it's legitimately unreachable right now.
What drifted / what went wrong: transition to=CONSOLIDATE requires prd-empty structurally, with zero distinction between a resolvable-by-more-work row and a genuinely blockedBy:external row (real signed-in Clerk session unavailable to this automated environment; upstream Next.js bug outside this session's control). Retried the bare transition 3 times across a session expecting a different outcome each time — the gate and the blocker are orthogonal, so retrying never helps.
Fix / resolution: Per wfgy-method's BBCR discipline, stop retrying the same denied verb after ~3 attempts. Document the stuck state as its own PRD row (consolidate-gate-external-blocker-standoff), hold at VERIFY, and report the standing blocked state to the user plainly instead of looping. Genuine forward progress (this turn's actual task: granting the platform-owner billing credits) still completed and got verified independently of the gm phase machine reaching COMPLETE.
Generalizes to: When gm's CONSOLIDATE/COMPLETE gate denies on PRD-open-items and every pending row is genuinely blockedBy:external (not a "try harder" gap), do NOT retry the transition more than ~3 times. Add the stuck-state PRD row, dispatch a real closing verb (phase-status), and report to the user — do not fabricate resolution to force the gate open.

## 2026-07-18 — gm-plugkit self-heal actively fights any pinned-version recovery
Goal (G): recover a working gm-plugkit watcher after a mid-session @latest auto-update broke it (missing ./wrapper/wasi-shim.js module, same defect class as the earlier gm-log.js incident).
What drifted / what went wrong: identified 0.1.906 (npm 2.0.1979) as the last fully self-consistent working wrapper+wasm pair, located a cached copy, downloaded a matching plugkit.wasm from the correct GitHub release tag, and manually placed both at ~/.claude/gm-tools/ with a self-consistent sha256 manifest — booted clean and processed a real dispatch successfully. Within minutes, the tool's own self-heal mechanism (triggered on the next supervisor/watcher restart cycle, independent of which npm version was invoked) overwrote the good wrapper.js with the broken @latest content again, re-entering the exact same crash loop (missing-module error, then a LinkError once a stale wasm got paired with it). Tried chmod 444 on the target files to block the overwrite — self-heal survived this too (almost certainly via a temp-file-then-rename swap, which only needs write permission on the parent directory, not the target file).
Fix / resolution: stopped retrying gm-plugkit recovery after ~5 genuinely distinct attempts (version pinning via bunx, full cache wipe, direct cli.js invocation bypassing @latest resolution, manual file restoration, chmod lockdown) — all defeated by the self-heal mechanism's own hardcoded fetch of a broken version. Documented as PRD row gm-plugkit-wasi-shim-and-abi-drift (pushed successfully during the brief working window). Proceeded with the user's actual task directly via standard Read/Edit/Bash tools, since the task itself (local launchd service recovery) has no dependency on gm-plugkit being available.
Generalizes to: when gm-plugkit's OWN self-heal mechanism is the thing re-introducing a known-bad state (not just a one-time bad fetch), further boot-authority retries are not the fix — the self-heal needs a real upstream fix (report to gm-plugkit maintainers) or a directory-level intervention (chmod the whole ~/.claude/gm-tools/ dir, not individual files) that this session judged too risky to attempt unilaterally given its broader side effects on legitimate gm-log/kv writes. Future sessions hitting this exact "self-heal keeps winning" pattern should recognize it fast (2 recurrences is enough signal) and fall back to direct tool use for the actual task rather than continuing to fight it.
## 2026-07-22 -- consolidate-gate vs blockedBy-external rows is a structural deadlock; BBCR-surface is the exit
Goal (G): ship the faithful @workflow/web console port fleet-wide, 100% verified, and close the gm chain cleanly.
What drifted / what went wrong: the CONSOLIDATE gate counts blockedBy:[external] PRD rows (upstream Next.js bug; GitHub account billing) as blocking residuals while the false-completion rule forbids resolving them — 7 identical gate denials accumulated across sessions before the loop was named.
Fix / resolution: exhausted bounded retries (2 this session), recorded the stuck state as its own PRD row, surfaced both external blockers to the user explicitly (GitHub Billing & plans fix; vercel/next.js#85490 watch), and parked the chain at VERIFY with everything in-reach resolved (43 rows), tree clean, pushed, prod-verified.
Generalizes to: when a gate and a correctness rule collide over genuinely-external work, do not orbit the gate — surface the contradiction and the external asks to the user; the chain resumes when the blockers clear. Also: check .ci-validated freshness (a stale head_sha from a prior session lingers) and remember GitHub Actions has been billing-dead since Jul 22 06:36 UTC — local CI-equivalents (release builds, workspace tests w/ podman machine started, tsc+next build) are the working gate until the user fixes billing.
## 2026-07-22 -- gm git_commit verb sweeps unrelated modified files despite paths-scoped git_add
Goal (G): commit ONLY the newly vendored ui/vendor/workflow-web tree while a live concurrent session held uncommitted edits in crates/hive-cloud/src/.
What drifted / what went wrong: git_add {paths:["ui/vendor"]} then git_commit produced commit 115aece containing the concurrent session's 3 modified crates/ files (admin.rs, gossip.rs, push.rs) alongside the 231 vendor files -- the verb commits all modified tracked files, not the staged set. Caught only because the VERIFY exec_js witness counted non-vendor files in the commit (3) instead of trusting the verb's OK response.
Fix / resolution: git reset --soft HEAD~1; git reset HEAD -- crates/; plain staged commit -> 0c85356 (231 ui/vendor files, 0 foreign; concurrent writer's edits restored to working tree unharmed). Raw-git deviation accepted deliberately: the verb pipeline cannot express a paths-scoped commit in a dirty tree.
Generalizes to: in a contended tree, never use the git_commit verb expecting staged-only semantics -- witness every commit's file list (git show --name-only) before treating it as done; also exec_js requires timeoutMs (positive int) or it hard-rejects.

## 2026-08-20 -- persisted CONSOLIDATE cannot admit a new executable task
Goal (G): make managed Supabase Studio load near-instantly while preserving every inherited PRD row and returning GM to a legal executable phase.
What drifted / what went wrong: a new user-authorized task landed while the persisted FSM remained in CONSOLIDATE with open PRD rows; the active graph has no CONSOLIDATE -> PLAN edge. The exact denial had already repeated five times across sessions. The gate-suggested recovery verb `fsm-propose-override` is not served by this running build (normal response window elapsed with no output), and the `codesearch`/`recall` verbs needed to discover its schema were likewise unavailable.
Fix / resolution: stopped after one newly-shaped supported-transition attempt plus one gate-suggested override attempt; preserved all rows, added `gm-consolidate-cannot-reenter-plan-with-open-prd`, and applied the established BBCR fallback: continue the actual measured task directly while keeping GM state visible and issuing instruction/phase-status heartbeats rather than mutating state files or deleting rows.
Generalizes to: a new request cannot safely reuse a globally persisted terminal-phase FSM. The graph needs a supported new-run/re-entry transition (COMPLETE/CONSOLIDATE -> PLAN when new PRD exists), and gate prose must not advertise an override verb absent from the running build.

## 2026-08-21 -- gm-plugkit daemon ABI mismatch (env::host_fs_cas_write) recurs under multi-session load
Goal (G): decide and drive to completion the Supabase Studio latency architecture (replicate-near-user vs multi-edge forward-proxy) using gm-orchestrated agents.
What drifted / what went wrong: the shared gm-plugkit daemon's queue was 86-deep and its watcher log 96 minutes stale under concurrent-session load; the documented single-shot escape hatch (`~/.gm-tools/agentplug-runner dispatch gm <verb> ...`) itself failed with `plugin instantiate failed: unknown import: env::host_fs_cas_write has not been defined` -- a fresh instance of the same self-heal/pinned-version drift class already recorded 2026-07-18.
Fix / resolution: did not keep retrying (already at bound per that entry's own precedent); proceeded with the actual engineering task directly via standard tools plus real parallel subagents instructed to follow this repo's own AGENTS.md discipline (live verification, no test files) since Skill(gm) inside a fresh subagent would hit the identical broken daemon.
Generalizes to: under heavy concurrent multi-session load, do not assume the shared daemon is healthy just because `.status.json` exists -- check `ts` staleness AND actually dispatch before trusting it; a stale watcher plus a broken direct-dispatch fallback together mean "unavailable this session," not "retry once more."
